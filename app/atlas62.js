window.ATLAS62=window.ATLAS62||(()=>{const state=(k,t,d="")=>`<div class="state62 ${k}"><i></i><b>${t}</b>${d?`<span>${d}</span>`:""}</div>`;function notify(m,t="info"){ATLAS52.toast(String(m),t)}function cleanup(){try{ATLAS54.stopAll()}catch{}}addEventListener("pagehide",cleanup);addEventListener("beforeunload",cleanup);return{state,loading:t=>state("loading",t||"Loading verified data…"),empty:(t,d)=>state("empty",t||"No verified data yet.",d||"ATLAS will fill this when the source provides it."),error:(t,d)=>state("error",t||"Data temporarily unavailable.",d||"Other panels can continue running."),notify,cleanup}})();