services:
  - type: web
    name: huddleintel-live
    runtime: python
    plan: free
    buildCommand: "echo No dependencies to install"
    startCommand: "python server.py"
    healthCheckPath: /api/status
