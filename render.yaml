services:
  - type: web
    name: nfl-2050-live
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: COLLECT_SECONDS
        value: "30"
      - key: DATABASE_URL
        sync: false
