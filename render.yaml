services:
  - type: web
    name: nfl-2050-live
    runtime: python
    plan: free
    buildCommand: "echo No dependencies to install"
    startCommand: "python server.py"
    healthCheckPath: /api/status
